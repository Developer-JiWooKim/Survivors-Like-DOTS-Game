# CLAUDE.md — 작업 지침

Unity 6 DOTS 기반 2D 뱀서라이크. 기획서는 [Docs/GameDesign.md](Docs/GameDesign.md).
이 프로젝트는 **기술 포트폴리오 / 면접 준비**가 목적 중 하나다. 결과물뿐 아니라 **과정과 판단 근거가 남는 것**이 동등하게 중요하다.

| | |
|---|---|
| Unity | 6000.6.0f1 |
| 렌더 | URP 17.6 (2D) |
| 핵심 스택 | Entities / Burst / Jobs / Collections (※ 아직 미설치, M0에서 설치) |
| 스크립트 루트 | `Assets/MyAssets/Scripts/` |

---

## 1. 작업 기록 (필수)

기록은 **두 문서로 분리**한다. 각 문서는 계속 누적하며, 더 쪼개지 않는다.

| 문서 | 담는 것 |
|---|---|
| [Docs/WorkLog.md](Docs/WorkLog.md) | **날짜별 작업 내용** — 그날 무엇을 했고 왜 그렇게 했는지 |
| [Docs/IssueLog.md](Docs/IssueLog.md) | **발생한 이슈와 대응** — 언제 무슨 문제가 났고 어떻게 처리했는지 |

두 문서는 **이슈 ID(`ISSUE-00X`)로 상호 참조**한다. WorkLog 엔트리 헤더에 그날 발생한 이슈 ID를 적고, IssueLog 엔트리에는 관련 WorkLog 날짜를 적는다.

### 1.1 WorkLog — 날짜별 작업 내용

**기록 시점**: 작업 세션을 마칠 때, 성능 측정을 했을 때, 설계 판단이 내려졌을 때

**엔트리에 반드시 포함**
1. **작업 날짜** (YYYY-MM-DD) 와 해당 마일스톤
2. **작업 내용** — 무엇을 만들었는지, 어떤 파일이 생기고 바뀌었는지
3. **왜 그렇게 했는지** — 고려한 대안, 선택한 이유, 포기한 이유, 트레이드오프
4. **성능 수치** — 측정했다면 before·after, 엔티티 수, ms, FPS, 측정 환경
5. **배운 것 / 다음에 다르게 할 것**
6. **커밋 해시** — 나중에 코드와 기록을 대조할 수 있도록

### 1.2 IssueLog — 이슈와 대응

**기록 시점**: 이슈가 **발생한 시점에 먼저 등록**하고(상태 `⏳ 진행중`), 해결되면 그 엔트리를 갱신한다.
해결된 뒤에 몰아서 쓰면 시도했다 실패한 과정이 통째로 날아간다. **그 과정이 가장 가치 있는 부분이다.**

**엔트리에 반드시 포함**
1. **이슈 ID** (`ISSUE-00X`, 순번 부여, 재사용 금지)
2. **발생 일시**와 마일스톤, 분류
3. **상태** — ✅해결 / 🔄우회 / ⏳진행중 / ⚠️미해결 / 🔁재발
4. **증상** — 에러 메시지 원문, 프로파일러 수치 등 **관측된 사실 그대로**
5. **재현 조건** — 항상/간헐적, 특정 엔티티 수 이상 등
6. **원인**
7. **시도한 것과 각각의 결과** — 실패한 시도 전부 포함
8. **해결 방법**과 **재발 방지책**
9. **해결 여부와 해결일**

문서 상단의 **이슈 목록 표를 항상 최신으로 유지**한다. 나중에 이 표만 훑어도 어떤 문제를 겪었는지 한눈에 보여야 한다.

### 1.3 기록 밀도

면접에서 "이 부분 왜 이렇게 하셨어요?" 라는 질문이 나왔을 때, **이 문서들만 다시 읽으면 답이 나오도록** 쓴다. 되돌아봤을 때 의미를 복원할 수 없는 요약은 기록하지 않은 것과 같다.

특히 **실패하고 되돌린 시도도 반드시 남긴다.** "A를 시도했으나 B 때문에 실패해서 C로 갔다"가 "C로 했다"보다 면접에서 훨씬 가치 있다.

---

## 2. 판단은 반드시 사용자에게 (필수)

**작업 중 정해야 할 사안이 생기면 스스로 판단하지 말고 반드시 물어본다.**

물어봐야 하는 것:
- 설계·구조 선택 (자료구조, 시스템 분리 방식, 아키타입 구성 등)
- 기획서에 명시되지 않은 수치·규칙
- 기획서와 실제 구현이 충돌할 때
- 라이브러리·패키지 추가
- 기존 코드를 크게 바꾸거나 되돌려야 할 때
- 성능 목표와 코드 단순성이 충돌할 때

물어볼 때는 **선택지와 각각의 트레이드오프를 정리해서 제시**한다. "어떻게 할까요?" 같은 열린 질문이 아니라, 판단에 필요한 재료를 갖춘 선택지를 준다. 추천안이 있으면 근거와 함께 밝히되, **결정은 사용자가 한다.**

막힌 사안이 있어도 그것과 무관한 작업은 먼저 진행한다.

---

## 3. 검증 — Unity CLI 사용

Claude는 Unity 배치모드 CLI로 **컴파일과 테스트를 직접 검증한다.** 사용자에게 떠넘기지 않는다.

```bash
bash Tools/unity-check.sh              # 컴파일 검증
bash Tools/unity-test.sh EditMode      # 테스트 실행
```

- 진입점: `Assets/MyAssets/Scripts/Editor/CI.cs` (`Assets.MyAssets.Scripts.Editor.CI.CompileCheck`)
- 로그·결과: `Logs/` (gitignore 처리됨)

### 3.1 반드시 지킬 것

- **C# 코드를 작성·수정한 뒤에는 `unity-check.sh` 를 돌린다.** 돌리기 전에 "컴파일된다"고 말하지 않는다.
- **에디터 락**: Unity는 한 프로젝트를 두 인스턴스가 열 수 없다. 에디터가 켜져 있으면 CLI가 실패한다(스크립트가 exit 2로 먼저 걸러줌). 이때는 **사용자에게 에디터를 닫아달라고 요청**한다. 임의로 프로세스를 죽이지 않는다.
- 최초 실행이나 패키지 설치 직후에는 임포트 때문에 수 분 걸릴 수 있다. 백그라운드로 돌린다.

### 3.2 CLI로 검증되지 않는 것 (사용자 확인 필요)

| 영역 | 이유 |
|---|---|
| 렌더링 결과, 드로우콜, GPU 시간 | `-nographics` 라 렌더 파이프라인이 돌지 않음 |
| 실제 프레임레이트 | 배치모드 수치는 빌드와 다름 |
| 재미·타격감·난이도 체감 | 당연히 불가 |

**M2 목표("적 1만 인스턴싱 렌더 60fps")의 렌더 쪽 절반은 CLI로 검증 불가**다. 사용자 실측이 필요하다.

### 3.3 씬은 사용자가, 에셋은 Claude가

| 대상 | 누가 |
|---|---|
| **씬 안의 오브젝트 배치·계층·컴포넌트 연결** | **사용자** (에디터에서 직접) |
| **에셋 파일 생성** — 머티리얼, ScriptableObject, 프리셋, 스프라이트 설정 등 | **Claude** (에디터 스크립트 + CLI) |
| C# 스크립트 (Authoring / System / MonoBehaviour) | Claude |

**씬 구성을 Claude가 자동화하지 않는 이유**: 에디터에서 직접 하는 게 빠르고, 결과를 눈으로 보면서 조정할 수 있다. 스크립트로 만들면 수정할 때마다 다시 돌려야 해서 오히려 느리다.

**에셋은 반대다**: 머티리얼 속성이나 ScriptableObject 값은 눈으로 조정할 게 별로 없고, 코드로 만들면 설정이 명시적으로 남아 재현 가능하다.

씬 작업을 요청할 때는 **에디터에서 뭘 눌러야 하는지 단계로** 적는다:
- 만들 GameObject 이름과 계층 구조
- 붙일 컴포넌트와 인스펙터에 넣을 값
- 참조로 연결해야 할 대상

- `*.meta`, `*.unity`, `*.prefab` 은 **직접 편집하지 않는다.** YAML이 깨지거나 GUID가 어긋나면 복구 비용이 크다. 에셋 생성은 `AssetDatabase` API 를 쓴다.
- 예외: `Packages/manifest.json` 은 직접 편집 가능. 단 편집 후 리졸브가 필요하다.
- 새 스크립트의 `.meta` 는 에디터/CLI 임포트가 생성한다. Claude가 만들지 않는다.
- 커밋에 `Library/`, `Temp/`, `Logs/` 가 들어가면 안 된다 (`.gitignore` 처리됨).

## 4. 성능 관련 원칙

- **측정하지 않은 성능은 주장하지 않는다.** "Job으로 바꿔서 빨라졌다"를 기록하려면 Unity Profiler 실측치가 있어야 한다. Burst/Job 성능은 직관과 자주 어긋난다.
- **성능 수치는 사용자가 에디터/빌드에서 측정한 값을 기준으로 한다.** CLI 배치모드 수치는 렌더가 빠져 있어 참고용으로만 쓰고, 기록에 남길 때는 반드시 출처를 명시한다.
- 측정 시 **항상 같은 조건**으로: 엔티티 수, 해상도, Editor/Build 구분, Burst on/off 를 함께 기록.
- 기획서 8.4의 아키타입 원칙(단일 아키타입 / 구조적 변경 금지 / 컴포넌트 최소화)을 어겨야 할 상황이 오면 **먼저 물어본다.**
- 최적화는 프로파일링 후에. 추측으로 선제 최적화하지 않는다. 단, 기획서에 명시된 구조적 결정(풀링, 공간 해시)은 예외 — 나중에 끼워넣기엔 비용이 너무 크다.

## 5. 코드 규약

- DOTS 시스템은 `ISystem` + `[BurstCompile]` 기본. managed 타입이 꼭 필요할 때만 `SystemBase`.
- **asmdef 를 사용한다.** 컴파일 시간과 Burst 설정 제어를 위해 필수.
  **어셈블리 이름도 네임스페이스와 동일하게 폴더 경로를 따른다.** 파일명도 어셈블리명과 일치시킨다.
  | asmdef | 위치 | 상태 |
  |---|---|---|
  | `Assets.MyAssets.Scripts.Editor` | `Scripts/Editor/` | 생성 완료 (CLI 진입점) |
  | `Assets.MyAssets.Scripts.Runtime` | `Scripts/Runtime/` | M0에서 생성 예정 |
  | `Assets.MyAssets.Scripts.Tests` | `Scripts/Tests/` | 필요 시점에 생성 |
- **테스트는 핵심 로직만.** 틀리면 디버깅이 지옥인 것에 한정한다: 공간 해시 조회 정확성, 지형 연소 확산 규칙, 데미지 집계. 나머지는 테스트하지 않는다 — 유지비가 개발 속도를 잡아먹는다.
- **네임스페이스는 폴더 경로를 그대로 따른다.** `Assets/` 부터 시작한다.
  | 파일 | 네임스페이스 |
  |---|---|
  | `Assets/MyAssets/Scripts/Test.cs` | `Assets.MyAssets.Scripts` |
  | `Assets/MyAssets/Scripts/Editor/CI.cs` | `Assets.MyAssets.Scripts.Editor` |
  | `Assets/MyAssets/Scripts/Runtime/Combat/DamageApplySystem.cs` | `Assets.MyAssets.Scripts.Runtime.Combat` |

  asmdef 의 `rootNamespace` 도 해당 폴더 기준으로 맞춰준다. 안 맞으면 IDE가 "네임스페이스가 폴더 구조와 일치하지 않습니다" 힌트를 띄운다.
- 클래스는 상속 의도가 없으면 `sealed`.
- 폴더 구조는 **기능 단위**로. 타입 단위(`Components/`, `Systems/` 전부 몰아넣기)는 시스템이 늘어나면 탐색이 불가능해진다.
  ```
  Scripts/Runtime/Combat/{DamageEvent.cs, DamageApplySystem.cs, ...}
  Scripts/Runtime/Terrain/{TileData.cs, TerrainTickSystem.cs, ...}
  ```
- 매직 넘버는 `IComponentData` 설정값 또는 상수로. 잡 안에 숫자를 박지 않는다.
- **코드 스타일은 [.editorconfig](.editorconfig) 가 강제한다.** 인덴트 4칸, Allman 중괄호, `private` 명시, private 필드 `_camelCase`. IDE가 알아서 맞춰주므로 따로 신경 쓰지 않는다.
- **주석은 한국어로, 왜(why)를 쓴다.** 무엇(what)은 코드가 말한다. 특히 DOTS는 "왜 이 구조여야 하는가"가 코드만 봐선 안 보이므로 중요하다.

## 6. 진행 방식

- **마일스톤 범위를 지킨다.** M2(적 1만 @60fps) 게이트를 통과하기 전에 M3 이후 코드를 쓰지 않는다. 기획서 10장 참조.
- **한 번에 한 덩어리씩.** 시스템 10개를 한꺼번에 쏟아내지 않는다. 하나 만들고 → 에디터에서 동작 확인 → 기록 → 다음.
- **커밋과 푸시는 사용자가 직접 한다.** Claude는 커밋하지 않는다.
- 커밋 메시지는 **Conventional Commits + 한국어**. 기능 단위로 나눈다.
  ```
  feat: 공간 해시 기반 적 충돌 판정 구현
  fix: 연소 확산이 대각선으로 번지는 문제 수정
  perf: 적 이동 잡을 IJobEntity 병렬로 전환
  refactor: 데미지 적용을 NativeStream 드레인 방식으로 변경
  docs: M2 성능 측정 결과 기록
  chore: Entities 패키지 설치
  test: 지형 연소 확산 규칙 테스트 추가
  ```
- 작업 기록에 **커밋 해시를 함께 남겨** 나중에 코드와 기록을 대조할 수 있게 한다.
- 답변은 한국어로.
